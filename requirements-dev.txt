# Dev/CI tooling only — handlers have no dependencies beyond the Lambda
# runtime's stdlib + boto3. Pin for reproducible CI.
ruff==0.16.1
pytest==9.1.1
cfn-lint==1.53.3
checkov==3.3.9
boto3==1.35.49
PyYAML==6.0.3
