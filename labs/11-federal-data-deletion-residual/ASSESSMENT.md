# Assessment Procedures — Federal Data Deletion Residual

Generated by `node scripts/build-assessment.mjs` from
`scf/scf-mapping.generated.json`. Do not edit by hand — CI gates on drift.

Each SCF control the lab claims is decomposed into an assessment objective, a
reproducible test procedure, and the expected evidence artifact. Procedures
invoke the lab's own handler; the pass/fail determination reads the
`compliance_status` field the handler emits (`PASS` / `FAIL` /
`CONFIG_ERROR` / `NOT_APPLICABLE`). Evidence-request IDs (e.g. `E-MON-01`)
are the SCF-defined artifacts and map to the JSON the handler persists to the
evidence bucket.

## How to run the check

```bash
# Offline behavior demonstration (no AWS calls):
python3 -c "import json,sys; sys.path.insert(0,'labs/11-federal-data-deletion-residual/src'); \
  import handler; from tests_shim import ctx; \
  print(handler.handler({'mode':'simulation'}, ctx()))"
# In-account: deploy infrastructure/template.yaml (sam deploy) and invoke the
# worker; inspect the evidence object written to the stack's evidence bucket.
```

## Control assessment matrix

| SCF control | Objective | Test procedure | Expected evidence | Framework citations |
|-------------|-----------|----------------|-------------------|---------------------|
| DCH-01 | Confirm the DCH objective "Data Protection" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-SVC-SIN) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CRY-01 | NIST 800-53 r5 MP-01; NIST 800-171 r3 03.01.01.d.01, 03.01.01.d.02, 03.08.01; PCI DSS 4.0.1 9.4, 9.4.1; FedRAMP r5 Mod MP-01 |
| DCH-09 | Confirm the DCH objective "System Media Sanitization" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-SVC-RUD, KSI-SVC-PRR) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-03, E-DCH-07 | NIST 800-53 r5 MP-06, MP-06(03); NIST 800-171 r3 03.07.04.c, 03.08.03; PCI DSS 4.0.1 9.4.7; FedRAMP r5 Mod MP-06, MP-06(03) |
| DCH-06 | Confirm the DCH objective "Media Storage" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-SVC-SIN) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-DCH-02, E-DCH-13 | NIST 800-53 r5 MP-04; NIST 800-171 r3 03.08.01; PCI DSS 4.0.1 9.1, 9.4, 9.4.1, 9.4.1.2; FedRAMP r5 Mod MP-04 |
| BCD-11 | Confirm the BCD objective "Data Backups" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-SVC-RUD) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-BCM-10, E-BCM-11, E-BCM-12, E-BCM-13 | NIST 800-53 r5 CP-09, SC-28(02); NIST 800-171 r3 03.08.09.a; PCI DSS 4.0.1 9.4.1.1, 9.4.1.2, 12.10.1; FedRAMP r5 Mod CP-09, SC-28(02) |
| CRY-05 | Confirm the CRY objective "Encrypting Data At Rest" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-SVC-SIN) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CRY-01 | NIST 800-53 r5 SC-13, SC-28, SC-28(01); NIST 800-171 r3 03.13.08; PCI DSS 4.0.1 3.3.2, 3.5, 3.5.1.2, 3.5.1.3, 8.3.2, 9.4; FedRAMP r5 Mod SC-13, SC-28, SC-28(01) |
| CLD-01 | Confirm the CLD objective "Cloud Services" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-AFR-PVL) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-06 | PCI DSS 4.0.1 1.2.1, 12.8.1 |

## Evidence handling

- **Where evidence lands:** `s3://<stack-evidence-bucket>/11-federal-data-deletion-residual/<yyyy>/<mm>/<dd>/<run-id>.json`
  (SSE-KMS, versioned, optional Object Lock).
- **Retention:** the evidence bucket lifecycle transitions to STANDARD_IA at
  90 days; log retention is set by the `LogRetentionDays` stack parameter.
- **Determination source:** `compliance_status` in the handler response and
  the persisted evidence object; `FAIL` also raises a Security Hub ASFF
  finding and an SNS alert.
- **Framework coverage note:** ISO 27001:2022, general-nist-800-171-r2, usa-federal-dow-cmmc-2-level-2 currently have no SCF crosswalk hits for this lab's controls (tracked in [COVERAGE.md](../../COVERAGE.md); resolved by the scheduled scf-refresh regeneration).
