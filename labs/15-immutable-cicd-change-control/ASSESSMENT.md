# Assessment Procedures — Immutable Cicd Change Control

Generated by `node scripts/build-assessment.mjs` from
`scf/scf-mapping.generated.json`. Do not edit by hand — CI gates on drift.

Each SCF control the lab claims is decomposed into an assessment objective, a
reproducible test procedure, and the expected evidence artifact. Procedures
invoke the lab's own handler; the pass/fail determination reads the
`compliance_status` field the handler emits (`PASS` / `FAIL` /
`CONFIG_ERROR` / `NOT_APPLICABLE`). Evidence-request IDs (e.g. `E-MON-01`)
are the SCF-defined artifacts and map to the JSON the handler persists to the
evidence bucket.

## How to run the check

```bash
# Offline behavior demonstration (no AWS calls):
python3 -c "import json,sys; sys.path.insert(0,'labs/15-immutable-cicd-change-control/src'); \
  import handler; from tests_shim import ctx; \
  print(handler.handler({'mode':'simulation'}, ctx()))"
# In-account: deploy infrastructure/template.yaml (sam deploy) and invoke the
# worker; inspect the evidence object written to the stack's evidence bucket.
```

## Control assessment matrix

| SCF control | Objective | Test procedure | Expected evidence | Framework citations |
|-------------|-----------|----------------|-------------------|---------------------|
| CHG-01 | Confirm the CHG objective "Change Management Program" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CMT-LMC) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CHG-02 | NIST 800-53 r5 CM-03; NIST 800-171 r3 03.04.02.b, 03.04.03.a; PCI DSS 4.0.1 1.2.2, 6.5, 6.5.1, 6.5.2, 6.5.3, 12.4.2; FedRAMP r5 Mod CM-03; ISO 27001:2022 6.3 |
| CHG-02 | Confirm the CHG objective "Configuration Change Control" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CMT-RVP) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CHG-02, E-CHG-05 | NIST 800-53 r5 CM-03, SA-08(31); NIST 800-171 r3 03.04.02.b, 03.04.03.a, 03.04.03.b, 03.04.03.c; PCI DSS 4.0.1 1.2.2, 6.5, 6.5.1, 6.5.6, 12.4.2; FedRAMP r5 Mod CM-03, SA-08(31) |
| CHG-03 | Confirm the CHG objective "Security Impact Analysis for Changes" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CMT-RMV) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CHG-04 | NIST 800-53 r5 CM-04; NIST 800-171 r3 03.04.03.b, 03.04.04.a, 03.04.11.b; PCI DSS 4.0.1 6.5.2, 6.5.6, A3.2.2, A3.2.3; FedRAMP r5 Mod CM-04 |
| CHG-04 | Confirm the CHG objective "Access Restriction For Change" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-AFR-SCN) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-HRS-13, E-IAM-02 | NIST 800-53 r5 CM-05; NIST 800-171 r3 03.04.02.b, 03.04.05; PCI DSS 4.0.1 1.2.8; FedRAMP r5 Mod CM-05 |
| CFG-01 | Confirm the CFG objective "Configuration Management Program" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CMT-RMV) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-01, E-AST-27 | NIST 800-53 r5 CM-01, CM-09; NIST 800-171 r3 03.04.01.a; PCI DSS 4.0.1 2.1, 2.2, 8.5; FedRAMP r5 Mod CM-01, CM-09 |
| TDA-06 | Confirm the TDA objective "Secure Software Development Practices (SSDP)" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CMT-VTD) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-TDA-08, E-TDA-11 | NIST 800-53 r5 SA-01, SA-04(03), SA-15; NIST 800-171 r3 03.16.01; PCI DSS 4.0.1 6.2, 6.2.1, 6.2.4; FedRAMP r5 Mod SA-01, SA-04(03), SA-15 |

## Evidence handling

- **Where evidence lands:** `s3://<stack-evidence-bucket>/15-immutable-cicd-change-control/<yyyy>/<mm>/<dd>/<run-id>.json`
  (SSE-KMS, versioned, optional Object Lock).
- **Retention:** the evidence bucket lifecycle transitions to STANDARD_IA at
  90 days; log retention is set by the `LogRetentionDays` stack parameter.
- **Determination source:** `compliance_status` in the handler response and
  the persisted evidence object; `FAIL` also raises a Security Hub ASFF
  finding and an SNS alert.
