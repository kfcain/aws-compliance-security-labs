# Assessment Procedures — Config Drift Compliance

Generated by `node scripts/build-assessment.mjs` from
`scf/scf-mapping.generated.json`. Do not edit by hand — CI gates on drift.

Each SCF control the lab claims is decomposed into an assessment objective, a
reproducible test procedure, and the expected evidence artifact. Procedures
invoke the lab's own handler; the pass/fail determination reads the
`compliance_status` field the handler emits (`PASS` / `FAIL` /
`CONFIG_ERROR` / `NOT_APPLICABLE`). Evidence-request IDs (e.g. `E-MON-01`)
are the SCF-defined artifacts and map to the JSON the handler persists to the
evidence bucket.

## How to run the check

```bash
# Offline behavior demonstration (no AWS calls):
python3 -c "import json,sys; sys.path.insert(0,'labs/04-config-drift-compliance/src'); \
  import handler; from tests_shim import ctx; \
  print(handler.handler({'mode':'simulation'}, ctx()))"
# In-account: deploy infrastructure/template.yaml (sam deploy) and invoke the
# worker; inspect the evidence object written to the stack's evidence bucket.
```

## Control assessment matrix

| SCF control | Objective | Test procedure | Expected evidence | Framework citations |
|-------------|-----------|----------------|-------------------|---------------------|
| CFG-01 | Confirm the CFG objective "Configuration Management Program" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CNA-EIS) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-01, E-AST-27 | NIST 800-53 r5 CM-01, CM-09; NIST 800-171 r3 03.04.01.a; PCI DSS 4.0.1 2.1, 2.2, 8.5; FedRAMP r5 Mod CM-01, CM-09 |
| CFG-02 | Confirm the CFG objective "Secure Baseline Configurations" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CNA-EIS) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-12, E-AST-13, E-AST-14, E-AST-15, E-AST-16, E-AST-17, E-AST-18, E-AST-19, E-AST-20, E-AST-21 | NIST 800-53 r5 CM-02, CM-06, PL-10, SA-08, SA-15(05); NIST 800-171 r3 03.01.01.h, 03.01.08.a, 03.01.08.b, 03.01.09, 03.01.10.a, 03.01.10.b, 03.01.10.c, 03.01.11, 03.01.12.a, 03.01.16.a, 03.01.18.a, 03.04.01.a, 03.04.02.a, 03.04.06.a, 03.04.06.b, 03.04.06.d, 03.05.07.d, 03.05.07.e, 03.05.07.f, 03.05.12.d, 03.08.07.a, 03.13.12.b; PCI DSS 4.0.1 1.1, 1.2.1, 1.2.6, 2.2, 2.2.1, 8.3.2, 8.5, 10.2, 10.2.1, 10.2.1.1, 10.2.1.2, 10.2.1.3, 10.2.1.4, 10.2.1.5, 10.2.1.6, 10.2.1.7, 10.2.2, 10.6, 10.6.1, 10.6.2, 10.6.3, 11.2; FedRAMP r5 Mod CM-02, CM-06, PL-10, SA-08, SA-15(05) |
| CPL-01 | Confirm the CPL objective "Statutory, Regulatory & Contractual Compliance" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-MLA-EVC) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CPL-01, E-GOV-10 | NIST 800-53 r5 PL-01, PM-08; NIST 800-171 r3 03.04.11.a, 03.12.01; PCI DSS 4.0.1 12.4, 12.4.2, A3.1, A3.1.1; FedRAMP r5 Mod PL-01, PM-08; ISO 27001:2022 4.1, 9.1, 9.2.1, 9.2.2 |
| CPL-02 | Confirm the CPL objective "Security, Compliance & Resilience Controls Oversight" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-MLA-EVC) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CPL-07, E-CPL-09, E-GOV-04, E-GOV-05, E-GOV-06, E-GOV-13, E-RSK-03 | NIST 800-53 r5 CA-07, CA-07(01), PM-14; NIST 800-171 r3 03.12.01, 03.12.03; PCI DSS 4.0.1 10.7, 10.7.1, 10.7.2, 10.7.3; FedRAMP r5 Mod CA-07, CA-07(01), PM-14; ISO 27001:2022 8.1, 10.1 |
| MON-01 | Confirm the MON objective "Continuous Monitoring" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-AFR-PVL) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-MON-01, E-MON-06, E-MON-07 | NIST 800-53 r5 AU-01, PM-31, SI-04; NIST 800-171 r3 03.03.01.a, 03.12.03, 03.14.06.a; PCI DSS 4.0.1 10.1, 10.4.3, 10.7, 10.7.1, 10.7.2, 10.7.3, A3.3.1, A3.5; FedRAMP r5 Mod AU-01, SI-04 |

## Evidence handling

- **Where evidence lands:** `s3://<stack-evidence-bucket>/04-config-drift-compliance/<yyyy>/<mm>/<dd>/<run-id>.json`
  (SSE-KMS, versioned, optional Object Lock).
- **Retention:** the evidence bucket lifecycle transitions to STANDARD_IA at
  90 days; log retention is set by the `LogRetentionDays` stack parameter.
- **Determination source:** `compliance_status` in the handler response and
  the persisted evidence object; `FAIL` also raises a Security Hub ASFF
  finding and an SNS alert.
