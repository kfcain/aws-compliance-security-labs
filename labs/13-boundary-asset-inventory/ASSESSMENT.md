# Assessment Procedures — Boundary Asset Inventory

Generated by `node scripts/build-assessment.mjs` from
`scf/scf-mapping.generated.json`. Do not edit by hand — CI gates on drift.

Each SCF control the lab claims is decomposed into an assessment objective, a
reproducible test procedure, and the expected evidence artifact. Procedures
invoke the lab's own handler; the pass/fail determination reads the
`compliance_status` field the handler emits (`PASS` / `FAIL` /
`CONFIG_ERROR` / `NOT_APPLICABLE`). Evidence-request IDs (e.g. `E-MON-01`)
are the SCF-defined artifacts and map to the JSON the handler persists to the
evidence bucket.

## How to run the check

```bash
# Offline behavior demonstration (no AWS calls):
python3 -c "import json,sys; sys.path.insert(0,'labs/13-boundary-asset-inventory/src'); \
  import handler; from tests_shim import ctx; \
  print(handler.handler({'mode':'simulation'}, ctx()))"
# In-account: deploy infrastructure/template.yaml (sam deploy) and invoke the
# worker; inspect the evidence object written to the stack's evidence bucket.
```

## Control assessment matrix

| SCF control | Objective | Test procedure | Expected evidence | Framework citations |
|-------------|-----------|----------------|-------------------|---------------------|
| AST-01 | Confirm the AST objective "Asset Governance" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-PIY-GIV) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-01 | NIST 800-53 r5 PM-05; NIST 800-171 r3 03.01.03, 03.01.18.a, 03.04.11.a, 03.07.04.a; PCI DSS 4.0.1 6.3.2, 9.5.1, 9.5.1.1, 11.2, 11.2.2; FedRAMP r5 Mod PM-05 |
| AST-02 | Confirm the AST objective "Asset Inventories" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-PIY-GIV) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-04, E-AST-05, E-AST-07, E-AST-28 | NIST 800-53 r5 CM-08, PM-05; NIST 800-171 r3 03.04.08.a, 03.04.08.c, 03.04.10.a, 03.04.10.b, 03.04.11.a; PCI DSS 4.0.1 6.3.2, 9.5.1, 9.5.1.1, 11.2, 11.2.2; FedRAMP r5 Mod CM-08, PM-05 |
| AST-04 | Confirm the AST objective "Network Diagrams & Data Flow Diagrams (DFDs)" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-AFR-MAS) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-DCH-03, E-DCH-04, E-DCH-05 | NIST 800-53 r5 PL-02, SA-04(01), SA-04(02); NIST 800-171 r3 03.01.03, 03.04.11.a, 03.04.11.b; PCI DSS 4.0.1 1.2.3, 1.2.4; FedRAMP r5 Mod PL-02, SA-04(01), SA-04(02) |
| NET-03 | Confirm the NET objective "Boundary Protection" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-AFR-MAS) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-NET-08, E-NET-09 | NIST 800-53 r5 SC-07, SC-07(09), SC-07(11); NIST 800-171 r3 03.01.12.a, 03.13.01.a, 03.13.01.b, 03.13.01.c; PCI DSS 4.0.1 1.3.3, 1.4, 1.4.1, 1.4.2, 11.5.1; FedRAMP r5 Mod SC-07, SC-07(09), SC-07(11) |
| CPL-01 | Confirm the CPL objective "Statutory, Regulatory & Contractual Compliance" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-MLA-LET) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CPL-01, E-GOV-10 | NIST 800-53 r5 PL-01, PM-08; NIST 800-171 r3 03.04.11.a, 03.12.01; PCI DSS 4.0.1 12.4, 12.4.2, A3.1, A3.1.1; FedRAMP r5 Mod PL-01, PM-08; ISO 27001:2022 4.1, 9.1, 9.2.1, 9.2.2 |
| GOV-01 | Confirm the GOV objective "Security, Compliance & Resilience Program (SCRP)" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-CNA-DFP) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-GOV-01, E-GOV-02 | NIST 800-53 r5 PM-01; NIST 800-171 r3 03.15.01.a; PCI DSS 4.0.1 12.4, A3.1.2; FedRAMP r5 Mod PM-01; ISO 27001:2022 4.4, 5.1, 5.1(a), 5.1(b), 5.1(c), 5.1(d), 5.1(e), 5.1(f), 5.1(g), 5.1(h), 6.1.1, 6.1.1(a), 6.1.1(b), 6.1.1(c), 6.1.1(d), 6.1.1(e)(1), 6.1.1(e)(2), 8.1, 10.1 |

## Evidence handling

- **Where evidence lands:** `s3://<stack-evidence-bucket>/13-boundary-asset-inventory/<yyyy>/<mm>/<dd>/<run-id>.json`
  (SSE-KMS, versioned, optional Object Lock).
- **Retention:** the evidence bucket lifecycle transitions to STANDARD_IA at
  90 days; log retention is set by the `LogRetentionDays` stack parameter.
- **Determination source:** `compliance_status` in the handler response and
  the persisted evidence object; `FAIL` also raises a Security Hub ASFF
  finding and an SNS alert.
