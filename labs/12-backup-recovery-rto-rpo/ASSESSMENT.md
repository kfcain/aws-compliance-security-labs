# Assessment Procedures — Backup Recovery Rto Rpo

Generated by `node scripts/build-assessment.mjs` from
`scf/scf-mapping.generated.json`. Do not edit by hand — CI gates on drift.

Each SCF control the lab claims is decomposed into an assessment objective, a
reproducible test procedure, and the expected evidence artifact. Procedures
invoke the lab's own handler; the pass/fail determination reads the
`compliance_status` field the handler emits (`PASS` / `FAIL` /
`CONFIG_ERROR` / `NOT_APPLICABLE`). Evidence-request IDs (e.g. `E-MON-01`)
are the SCF-defined artifacts and map to the JSON the handler persists to the
evidence bucket.

## How to run the check

```bash
# Offline behavior demonstration (no AWS calls):
python3 -c "import json,sys; sys.path.insert(0,'labs/12-backup-recovery-rto-rpo/src'); \
  import handler; from tests_shim import ctx; \
  print(handler.handler({'mode':'simulation'}, ctx()))"
# In-account: deploy infrastructure/template.yaml (sam deploy) and invoke the
# worker; inspect the evidence object written to the stack's evidence bucket.
```

## Control assessment matrix

| SCF control | Objective | Test procedure | Expected evidence | Framework citations |
|-------------|-----------|----------------|-------------------|---------------------|
| BCD-01 | Confirm the BCD objective "Business Continuity Management System (BCMS)" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-RPL-ARP, KSI-CNA-OFA) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-BCM-01 | NIST 800-53 r5 CP-01, CP-02, CP-10, IR-04(03), PM-08; FedRAMP r5 Mod CP-01, CP-02, CP-10, IR-04(03), PM-08 |
| BCD-02 | Confirm the BCD objective "Identify Critical Assets" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-RPL-RRO) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-BCM-08 | NIST 800-53 r5 CP-02(08); FedRAMP r5 Mod CP-02(08) |
| BCD-11 | Confirm the BCD objective "Data Backups" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-RPL-ABO) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-BCM-10, E-BCM-11, E-BCM-12, E-BCM-13 | NIST 800-53 r5 CP-09, SC-28(02); NIST 800-171 r3 03.08.09.a; PCI DSS 4.0.1 9.4.1.1, 9.4.1.2, 12.10.1; FedRAMP r5 Mod CP-09, SC-28(02) |
| BCD-12 | Confirm the BCD objective "Technology Assets, Applications and/or Services (TAAS) Recovery & Reconstitution" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-RPL-TRC) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-BCM-15 | NIST 800-53 r5 CP-10; FedRAMP r5 Mod CP-10 |
| CRY-05 | Confirm the CRY objective "Encrypting Data At Rest" is operating: the handler evaluates live posture and returns a determination. (KSI KSI-RPL-ABO) | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-CRY-01 | NIST 800-53 r5 SC-13, SC-28, SC-28(01); NIST 800-171 r3 03.13.08; PCI DSS 4.0.1 3.3.2, 3.5, 3.5.1.2, 3.5.1.3, 8.3.2, 9.4; FedRAMP r5 Mod SC-13, SC-28, SC-28(01) |
| AST-02 | Confirm the AST objective "Asset Inventories" is operating: the handler evaluates live posture and returns a determination. | Invoke the lab worker; verify `compliance_status` reflects real posture (unconfigured inputs must yield `CONFIG_ERROR`, never `PASS`). | E-AST-04, E-AST-05, E-AST-07, E-AST-28 | NIST 800-53 r5 CM-08, PM-05; NIST 800-171 r3 03.04.08.a, 03.04.08.c, 03.04.10.a, 03.04.10.b, 03.04.11.a; PCI DSS 4.0.1 6.3.2, 9.5.1, 9.5.1.1, 11.2, 11.2.2; FedRAMP r5 Mod CM-08, PM-05 |

## Evidence handling

- **Where evidence lands:** `s3://<stack-evidence-bucket>/12-backup-recovery-rto-rpo/<yyyy>/<mm>/<dd>/<run-id>.json`
  (SSE-KMS, versioned, optional Object Lock).
- **Retention:** the evidence bucket lifecycle transitions to STANDARD_IA at
  90 days; log retention is set by the `LogRetentionDays` stack parameter.
- **Determination source:** `compliance_status` in the handler response and
  the persisted evidence object; `FAIL` also raises a Security Hub ASFF
  finding and an SNS alert.
- **Framework coverage note:** ISO 27001:2022, general-nist-800-171-r2, usa-federal-dow-cmmc-2-level-2 currently have no SCF crosswalk hits for this lab's controls (tracked in [COVERAGE.md](../../COVERAGE.md); resolved by the scheduled scf-refresh regeneration).
