{
  "name": "aws-compliance-security-labs",
  "version": "2.0.0",
  "private": true,
  "description": "Giant monorepo combining all AWS compliance & security lab-* projects",
  "scripts": {
    "scf:test": "node --test shared/scf-mapper/src/mapper.test.js",
    "scf:map:all": "node scripts/map-all-labs.mjs",
    "list-labs": "node -e \"console.log(require('./labs/catalog.json').labs.map(l=>l.id).join('\\n'))\""
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
